import { WaypointsFilterPipe } from './waypoints-filter.pipe';

describe('WaypointsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WaypointsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
