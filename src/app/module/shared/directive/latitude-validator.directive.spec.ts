import { LatitudeValidatorDirective } from './latitude-validator.directive';

describe('LatitudeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LatitudeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
