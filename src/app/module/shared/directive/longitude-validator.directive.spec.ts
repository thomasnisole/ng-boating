import { LongitudeValidatorDirective } from './longitude-validator.directive';

describe('LongitudeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LongitudeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
